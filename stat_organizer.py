from stat_node import StatNode

#this would be the class that does all the managment of any stats on the player(the ai )
class StatOrganizer():
    def __init__(self):
        stats = self.load_stats()
    def load_stats():
        return 10
    #this would load all the stat nodes into a list maybe? store them so and then process what they do to the ai in here 
    
    

    


    